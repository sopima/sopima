services:
  app:
    image: ghcr.io/sopima/sopima:latest
    build: .
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - sopima_storage:/var/www/html/storage

volumes:
  sopima_storage:
